services:
  frontend:
    build: 
      context: ./ts-front
    container_name: ts-frontend
    ports:
      - "5173:5173"
    networks:
      - textifying-speaking-network
    depends_on:
      - backend

  backend:
    build: 
      context: ./ts-back
    container_name: ts-backend
    ports:
      - "3001:3001"
    networks:
      - textifying-speaking-network

networks:
  textifying-speaking-network:
